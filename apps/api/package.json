{
  "name": "@civil-terrain/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@civil-terrain/adapters": "workspace:*",
    "@civil-terrain/db": "workspace:*",
    "@civil-terrain/domain": "workspace:*"
  },
  "devDependencies": {
    "@civil-terrain/geo": "workspace:*",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8",
    "wrangler": "^4.111.0"
  }
}
